# EmailJS configuration used by the contact form
VITE_EMAILJS_SERVICE_ID=your_service_id
VITE_EMAILJS_TEMPLATE_ID=your_template_id
VITE_EMAILJS_PUBLIC_KEY=your_public_key

# Cloudflare (server-side only; do NOT commit real values, do NOT prefix with VITE_)
# Add these as GitHub Actions secrets (CF_API_TOKEN, CF_ACCOUNT_ID) and optionally locally for wrangler CLI.
CF_ACCOUNT_ID=your_account_id
CF_API_TOKEN=your_api_token
